# Production Environment Variables
# Copy this file and set appropriate values for your production environment

# =============================================================================
# Backend (VPS + Coolify)
# =============================================================================

# Node environment
NODE_ENV=production

# Server port
PORT=3001

# Database connection (internal Docker network)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://app:YOUR_STRONG_PASSWORD@postgres:5432/app

# CORS allowed origins (comma-separated)
# Example: https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=https://yourdomain.com

# =============================================================================
# Docker Compose (for Coolify)
# =============================================================================

# PostgreSQL password (used by docker-compose.production.yml)
DB_PASSWORD=YOUR_STRONG_PASSWORD

# =============================================================================
# Frontend (Cloudflare Pages - set in dashboard)
# =============================================================================

# API URL for frontend to call backend
# VITE_API_URL=https://api.yourdomain.com
